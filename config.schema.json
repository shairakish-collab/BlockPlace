{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "version": { "type": "integer" },
    "enabled": { "type": "boolean", "description": "Whether random placement is currently active" }
  },
  "required": ["version", "enabled"]
}
